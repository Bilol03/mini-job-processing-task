export enum RoleEnum {
    ADMIN="ADMIN",
    USER="USER"
}

export enum TaskPriority {
  HIGH = "high",
  NORMAL = "normal",
  LOW = "low",
}

export enum TaskStatus {
  PENDING = "PENDING",
  PROCESSING = "PROCESSING",
  COMPLETED = "COMPLETED",
  FAILED = "FAILED",
  CANCELLED = "CANCELLED",
}